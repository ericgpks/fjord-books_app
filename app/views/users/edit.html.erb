<h1><%= t('controllers.common.editing', name: User.model_name.human) %></h1>

<%= render 'form', user: @user %>

<%= link_to t('views.common.show'), @user %> |
<%= link_to t('views.common.back'), users_path %>
